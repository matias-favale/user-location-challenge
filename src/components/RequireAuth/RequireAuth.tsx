// import {useState} from "react";

const RequireAuth = () => {
    // const [isLoading, setIsLoading] = useState(true)

    // Call login endpoint

    // If user data is present show it
    // If user data is not present request for new data

    return (
        <></>
    )
}

export default RequireAuth